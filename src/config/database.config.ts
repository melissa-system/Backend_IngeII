import { registerAs } from '@nestjs/config';
import { TypeOrmModuleOptions } from '@nestjs/typeorm';
/*
export default registerAs('database', (): TypeOrmModuleOptions => ({
 // Esto creará las tablas automáticamente en desarrollo
}));*/